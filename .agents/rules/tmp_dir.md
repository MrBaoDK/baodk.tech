# Temporary Directory Usage

## .tmp Directory
- The `.tmp/` directory is allowed for running temporary scripts and storing transient data.
- Files in `.tmp/` should be git-ignored by default.
- Agents are allowed to read/write to this directory for task-specific temporary needs.
